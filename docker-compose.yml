services:
  postgres:
    image: postgres:16
    container_name: futsi-postgres
    environment:
      POSTGRES_DB: futsi
      POSTGRES_USER: futsi
      POSTGRES_PASSWORD: futsi
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
